# Environment Variables for Docker Compose
# Copy this file to .env and update with your values

# Database password for Azure SQL Edge
# Must meet SQL Server password requirements:
# - At least 8 characters
# - Contains uppercase, lowercase, numbers, and special characters
DB_PASSWORD=YourStrong!Passw0rd

# Kafka SASL Credentials (for production deployments)
# These are used by the application when connecting to secure Kafka clusters
# In development, these default to simple values defined in application.yml
# In production, set these to strong, unique passwords
KAFKA_USERNAME=producer
KAFKA_PASSWORD=producer-secret
