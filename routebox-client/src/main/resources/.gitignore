# This .gitignore file keeps this directory in version control
# while allowing it to remain empty or contain only local resources.
# This prevents Maven from logging a warning about a missing resources directory.
