spring:
  config:
    activate:
      on-profile: verbose-logging

logging:
  config: classpath:logback-verbose.xml
  level:
    root: INFO
    org.springframework: INFO
    org.hibernate: INFO
    org.testcontainers: INFO
    tc: INFO
