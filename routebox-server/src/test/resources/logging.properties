# Java Util Logging configuration for tests
# Suppress noisy SQL Server JDBC driver warnings during test container startup

# Global log level
.level=WARNING

# Suppress SQL Server JDBC connection warnings
com.microsoft.sqlserver.jdbc.level=SEVERE
