# Maven configuration for batch mode execution
# -B, --batch-mode: Run in non-interactive (batch) mode
# -ntp, --no-transfer-progress: Do not display transfer progress when downloading or uploading

--batch-mode
--no-transfer-progress
